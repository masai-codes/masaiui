/**
 * 📝 Notes for Contributors:
 * 
 * - When creating an interactive component, we recommend consuming the
 * component hook created.
 * 
 * For example, if you wanted to build an accordion component,
 * you'll first create a `useAccordion` hook and then create an `Accordion` component
 * 
 * - Ensure this component is properly themable by following [this guide](FIXME:)
 * 
 * - Ensure the component is composable and can adapt to multiple use-cases
 * 
 * @see Guide FIXME:
 * @see Theming FIXME:
 */

import * as React from "react"
import { HTMLProps } from "@masaiui/system"

export interface {{capitalize componentName}}Props extends HTMLProps<"div"> {
}

export function {{capitalize componentName}}(props: {{capitalize componentName}}Props) {
  return <div/>;
}